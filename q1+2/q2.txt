I will instantly take the misconfigured API endpoint offline or strictly limit access (with admin authentication) to stop any further data exposure, then check the server access logs to determine exactly what sensitive volunteer data was accessed, who accessed it, and the timeframe of the exposure, then notify OC and faculty advisor immediately, then identify the bug in the code, and update it and finally test the final code.
